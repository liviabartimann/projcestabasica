import React from 'react';

export default function inputPassword() {
    return (
        <div class="password">
            <label>
                Password:
            <input type="password" />
            </label>
        </div>
    );
}
